using System;
using System.Threading;

namespace VehicleLocator
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.Title = "MIX Telematics Vehicle Locator" ;
            Console.ForegroundColor = ConsoleColor.Magenta;
            Console.WriteLine("Welcome to the Mix Vehicle Locator!");

            /*
             * Store file in reader. Start with first 10 results then once code works, read entire file.  
             * Store co-ordinates in file and read them into a List.  
             * 
             */

            
        }

        private void ReadVehiclePositions()
        {
            var fileName = "C:\\src\\MixTelematics\\VehicleLocator\VehicleLocator\VehicleLocator";

        }

        private void ReadCoOrdinates()
        {

        }

        private void GetClosestVehicleToCoOrdinate()
        {

        }

        private void DisplayResults()
        {

        }
    }
}
